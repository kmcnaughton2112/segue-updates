# segue updates

This repository holds one small file, `version.json`, that the segue app reads when it launches to learn whether a newer version is available. It contains only a version number and a link.

Nothing here is code, and the app never downloads or installs anything from this repository.
